<!-- <% if current_user.follows?(user) %>
  <%= button_to "Unfollow", user_follow_url(user), method: :delete %>
<% else %>
  <%= button_to "Follow", user_follow_url(user), method: :post %>
<% end %> -->


<% follow_state = (current_user.follows?(user) ? "following" : "notFollowing") %>

<button type="button" class="follow-toggle" data-follow-state="<%= follow_state %>" data-user-id="<%= user.id %>"></button>
